@model Accounting.Models.BlogViewModels.CreateBlogViewModel

@{
  ViewData["Title"] = "Create post - " + ConfigurationSingleton.Instance.ApplicationName;
}

<script src="~/js/vue-3-5-13.js" asp-append-version="true"></script>

@{
  string headerText = "Create post";
}
<partial name="_H1HeaderPartial" model="@headerText" />

<form id="app" asp-action="Create" asp-controller="Blog" method="post" class="display-flex flex-direction-column margin-bottom-5px">
  <div class="display-flex flex-direction-column margin-bottom-5px">
    <label asp-for="Title" class="background-color-antiquewhite"></label>
    <input asp-for="Title" class="font-size-20px" maxlength="100" autocomplete="off" />
  </div>
  <div class="display-flex flex-direction-column margin-bottom-5px background-color-antiquewhite">
    <label asp-for="Content" class="background-color-antiquewhite"></label>
    <textarea asp-for="Content" class="font-size-20px" rows="6"></textarea>
    <span>
      <input asp-for="Public" type="checkbox" class="margin-bottom-5px" />
      <label asp-for="Public">public</label>
    </span>
  </div>
  <div class="display-flex flex-direction-column margin-bottom-5px background-color-antiquewhite">
    <div class="flex-1 padding-5px background-color-antiquewhite">
      <input asp-for="File" type="file" @@change="uploadFile" ref="fileInput" />
    </div>
  </div>
  <button type="submit" class="full-width-regular-button">Create post</button>
</form>
@* 
<script>
  const { createApp } = Vue;



  const app = createApp({
    components: {
      attachments: @Html.Raw(Model?.Blo
    },
    data() {
      return {
        file: null
      };
    },
    methods: {
      uploadFile(event) {
        this.file = event.target.files[0];
      }
    }
  });

  app.mount('#app');
</script> *@