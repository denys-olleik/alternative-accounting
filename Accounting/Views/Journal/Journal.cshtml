@model Accounting.Models.JournalViewModels.JournalPaginatedViewModel

@{
  ViewData["Title"] = "Journal - " + ConfigurationSingleton.Instance.ApplicationName;
}

@{
  string headerText = "Journal";
}

<partial name="_H1HeaderPartial" model="@headerText"/>

<div id="app">
  <table class="border-collapse width-100 margin-bottom-5px background-color-antiquewhite">
    <thead class="border-bottom-1px-solid-1a3b56 font-Roboto">
    <tr>
      <th>Transaction</th>
    </tr>
    </thead>
    <tbody>
    <template v-for="journal in journals" :key="journal.journalID">
      <tr>
        <td>{{ journal.transactionGuid }}</td>
      </tr>
    </template>
    </tbody>
  </table>
</div>

<script>
  const { createApp } = Vue;
  
  const app = createApp({
    components: {
      
    },
    data() {
      return {
        journals: [],
        page: @Model.Page,
        pageSize: @Model.PageSize,
        nextPage: null,
        rememberPageSize: @(Model.RememberPageSize ? "true" : "false")
      }
    },
    methods: {
      fetchJournals() {
        const storedPageSize = parseInt(localStorage.getItem('pageSize'));
        if (storedPageSize) {
          this.pageSize = storedPageSize;
        }

        let apiUrl = `/api/journals?page=${this.page}&pageSize=${this.pageSize}`;

        fetch(apiUrl)
          .then(response => response.json())
          .then(data => {
            this.journals = data.journals;
            if (data.page > 0) {
              this.page = data.page;
            }
            if (data.nextPage > 0) {
              this.nextPage = data.nextPage;
            } else {
              this.nextPage = null;
            }
          });
      }
    },
    mounted() {
      if (this.rememberPageSize) {
        localStorage.setItem('pageSize', this.pageSize);
      }
      this.fetchJournals();
    }
  })
  
  app.mount('#app');
</script>